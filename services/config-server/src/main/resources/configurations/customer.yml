spring:
    data:
      mongodb:
        username: Microservices_Customer
        password: root
        host: localhost
        port: 27017
        database: customer
        authentication-database: admin
server:
  port: 8090

  spring:
    application:
      name: customer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
